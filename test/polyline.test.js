// Test various polyline-related algorithms

function validatePolyline (pVertices, triangleStrip, pen) {
  // Essentially, we choose various random vertices surrounding the polyline, compute their distances to the polyline,
  // and, if they're close enough, make sure they are within the triangle strip


}
